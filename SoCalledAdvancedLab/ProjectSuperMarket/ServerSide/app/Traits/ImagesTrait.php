<?php

namespace \App\Traits;

trait ImagesTrait {
	public storeImages ($images) {
		if count($images) == 1 {
			$images = [$images];
		}

		foreach ($images as $name => $image) {
			if () {
				# code...
			}
		}
	}
}
